# Core dependencies
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# ML and data processing
scikit-learn>=1.3.0
pandas>=2.0.0
tqdm>=4.65.0

# Configuration and logging
pyyaml>=6.0
tensorboard>=2.13.0

# TTM model dependencies
transformers>=4.30.0
huggingface-hub>=0.16.0

# Granite Time Series Foundation Model (IBM TTM) - REQUIRED
# This is a HARD dependency - the repository will NOT work without it
tsfm_public @ git+https://github.com/ibm-granite/granite-tsfm.git

# Development dependencies (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
isort>=5.12.0
mypy>=1.4.0
